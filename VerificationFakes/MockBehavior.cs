namespace VerificationFakes
{
    /// <summary>
    /// Options to customize the behavior of the mock.
    /// </summary>
    public enum MockBehavior
    {
        Default = 0,
        Loose = 0,
        Strict = 1,
    }
}